# 后端环境变量配置

# 音乐库目录配置
# 如果不设置，会自动尝试以下路径：
# 1. F:\Music (Windows)
# 2. 用户主目录下的 Music 文件夹
# 例如: C:\Users\YourName\Music
#
# 设置方法（Windows）:
# MUSIC_DIR=F:\Music
#
# 设置方法（Linux/Mac）:
# MUSIC_DIR=/path/to/your/music

# 禁用本地音乐库（仅使用BGM）
# 设置为 true 时，不加载本地音乐，只使用背景音乐
# DISABLE_LOCAL_MUSIC=false

# 服务器端口
PORT=3000

# Socket.IO 跨域设置
CORS_ORIGIN=http://localhost:5173
